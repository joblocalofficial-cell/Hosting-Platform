# 📘 คู่มือการติดตั้ง Google Apps Script

## 🎯 สิ่งที่ต้องเตรียม
1. บัญชี Google
2. Google Sheets ที่จะใช้เก็บข้อมูล
3. ไฟล์ `Code.gs` (มีอยู่ในโปรเจคนี้แล้ว)

---

## 📝 ขั้นตอนการติดตั้ง

### **ขั้นตอนที่ 1: สร้าง Google Sheets**
1. ไปที่ [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่ (กดปุ่ม + หรือ Blank)
3. ตั้งชื่อว่า **"JobLocal Database"** หรือชื่อที่คุณต้องการ
4. **คัดลอก Spreadsheet ID** จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
   ```
   ตัวอย่าง: `1AbC_dEfGhIjKlMnOpQrStUvWxYz`

---

### **ขั้นตอนที่ 2: สร้าง Google Apps Script Project**

#### วิธีที่ 1: ผ่าน Google Sheets (แนะนำ)
1. เปิด Google Sheets ที่สร้างไว้
2. ไปที่เมนู **Extensions** → **Apps Script**
3. จะเปิดหน้าต่างใหม่ของ Apps Script Editor

#### วิธีที่ 2: ผ่าน Apps Script โดยตรง
1. ไปที่ [script.google.com](https://script.google.com)
2. กดปุ่ม **New Project**

---

### **ขั้นตอนที่ 3: เพิ่มโค้ด**
1. ลบโค้ดเริ่มต้นทั้งหมดออก
2. **คัดลอกโค้ดทั้งหมดจากไฟล์ `Code.gs`** ในโปรเจคนี้
3. วางลงใน Apps Script Editor
4. **แก้ไขบรรทัดที่ 9:**
   ```javascript
   const SPREADSHEET_ID = "YOUR_SPREADSHEET_ID_HERE";
   ```
   เปลี่ยนเป็น:
   ```javascript
   const SPREADSHEET_ID = "1AbC_dEfGhIjKlMnOpQrStUvWxYz";  // ใส่ ID จริงของคุณ
   ```
5. กดปุ่ม **💾 Save** (Ctrl+S / Cmd+S)
6. ตั้งชื่อโปรเจคเป็น **"JobLocal Backend"**

---

### **ขั้นตอนที่ 4: ทดสอบการทำงาน (Optional แต่แนะนำ)**
1. เลือกฟังก์ชัน **testSetup** จาก dropdown
2. กดปุ่ม **▶️ Run**
3. ครั้งแรกจะขออนุญาต:
   - กด **Review permissions**
   - เลือกบัญชี Google ของคุณ
   - กด **Advanced** → **Go to [ชื่อโปรเจค] (unsafe)**
   - กด **Allow**
4. ตรวจสอบ Google Sheets ควรมี 2 sheets:
   - ✅ **ผู้หางาน**
   - ✅ **นายจ้าง**

---

### **ขั้นตอนที่ 5: Deploy เป็น Web App**
1. กดปุ่ม **🚀 Deploy** → **New deployment**
2. กดปุ่ม **⚙️ Settings icon** (ข้างๆ Select type)
3. เลือก **Web app**
4. ตั้งค่าดังนี้:
   - **Description:** `JobLocal API v1` (หรือชื่อที่ต้องการ)
   - **Execute as:** `Me`
   - **Who has access:** `Anyone` ⚠️ สำคัญ!
5. กดปุ่ม **Deploy**
6. **คัดลอก Web app URL** ที่ได้
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```

---

### **ขั้นตอนที่ 6: อัปเดต URL ในไฟล์ HTML**

#### ในไฟล์ `jobseeker.html` (บรรทัดที่ 210):
```javascript
const GAS_ENDPOINT = "https://script.google.com/macros/s/YOUR_NEW_URL/exec";
```

#### ในไฟล์ `employee.html` (บรรทัดที่ 235):
```javascript
const GAS_ENDPOINT = "https://script.google.com/macros/s/YOUR_NEW_URL/exec";
```

⚠️ **เปลี่ยน URL ให้ตรงกับที่ได้จาก deployment!**

---

## ✅ การทดสอบ

### ทดสอบผ่าน Browser Console:
1. เปิดไฟล์ HTML ในเบราว์เซอร์
2. เปิด Developer Tools (F12)
3. กรอกฟอร์มและส่งข้อมูล
4. ตรวจสอบ Console ว่ามี:
   ```
   ✅ Success!
   ```
5. ตรวจสอบ Google Sheets ว่ามีข้อมูลเพิ่มขึ้น

---

## 🔧 การอัปเดตโค้ด (ในอนาคต)

หากแก้ไขโค้ดใน `Code.gs`:
1. แก้ไขโค้ดใน Apps Script Editor
2. กด **💾 Save**
3. ไปที่ **🚀 Deploy** → **Manage deployments**
4. กดปุ่ม **✏️ Edit** (ที่ deployment ปัจจุบัน)
5. เปลี่ยน **Version** เป็น **New version**
6. กด **Deploy**
7. **URL จะไม่เปลี่ยน** แต่โค้ดจะอัปเดต

---

## 🐛 การแก้ปัญหา

### ❌ CORS Error
- ตรวจสอบว่าตั้งค่า **Who has access: Anyone**
- ตรวจสอบว่า URL ถูกต้อง

### ❌ ส่งข้อมูลไม่สำเร็จ
1. เปิด Apps Script Editor
2. ไปที่ **Executions** (ด้านซ้าย)
3. ดูข้อผิดพลาดที่เกิดขึ้น
4. หรือดู **Logs** เพื่อดู debug messages

### ❌ ไม่มีข้อมูลใน Sheet
- ตรวจสอบ `SPREADSHEET_ID` ในโค้ด
- ตรวจสอบว่า execution ไม่มี error ใน Apps Script

---

## 📞 สรุป Checklist

- [ ] สร้าง Google Sheets และคัดลอก Spreadsheet ID
- [ ] สร้าง Apps Script Project
- [ ] วางโค้ดจาก `Code.gs` และแก้ไข `SPREADSHEET_ID`
- [ ] รันฟังก์ชัน `testSetup` เพื่อทดสอบ
- [ ] Deploy เป็น Web App (Anyone access)
- [ ] คัดลอก Web App URL
- [ ] อัปเดต `GAS_ENDPOINT` ใน `jobseeker.html`
- [ ] อัปเดต `GAS_ENDPOINT` ใน `employee.html`
- [ ] ทดสอบการส่งฟอร์ม
- [ ] ตรวจสอบข้อมูลใน Google Sheets

---

## 🎉 เสร็จสิ้น!

หากทำตามขั้นตอนครบถ้วน ระบบควรทำงานได้แล้วครับ!

หากมีปัญหาเพิ่มเติม สามารถตรวจสอบ:
- Console ในเบราว์เซอร์ (F12)
- Logs ใน Apps Script Editor
- Executions ใน Apps Script Editor
